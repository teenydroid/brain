Task: Fold the new signals and the settled runs into learnings; update Goals.md to match the goals ledger.

## Input from the previous step
recorded 4 signals from what i found looking around:

1. **all-ledgers-empty** — goals, decisions, commitments, learnings, risks, changes, signals all at zero rows. the company has no recorded goals, decisions, or risks yet.

2. **tasks-all-pending** — 8 task cards on the board, all in "pending". highest priority: write the company brief, set first goals, write first goals, run first reflect.

3. **goals-md-empty** — Goals.md in the workspace says "Nothing yet. The first task on the board is to write these." matches the empty goals ledger.

4. **workspace-sparse** — 8 workspace entries, no brief.md, agents folder empty. git shows a brain sync commit at 11:29 UTC today.

the reflect playbook says the pass runs daily at 06:00 UTC: scout looks → scribe learns → curator revises. this is the scout looking. the scribe and curator haven't run yet.

**still to do:** the scribe needs to read these signals and decide what they mean, then the curator revises if a change is needed. also none of the pending task cards have been started yet — the board is sitting untouched.
Outcome: Goals.md already says "Nothing yet" and the goals ledger is empty — they're in sync, no update needed there. And nothing in the signals suggests the definition is wrong, so `change_needed` stays false — no curator pass needed.

Here's what I recorded:

**learning: company-initialized** (status: noted) — the company was created today, all ledgers empty, 8 pending cards, workspace sparse. The signals all converge on that. One row captures it cleanly.

The board already has the right next steps prioritized: write the brief → set goals → write goals → run reflect. Nothing for me to add there.

**reflect pass summary:** scout saw 4 signals → scribe recorded 1 learning, confirmed Goals.md matches the goals ledger, no definition change needed. The loop closes without a curator step.